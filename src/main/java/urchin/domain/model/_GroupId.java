package urchin.domain.model;

import org.immutables.value.Value;
import urchin.domain.model.wrapper.Wrapped;
import urchin.domain.model.wrapper.Wrapper;

@Value.Immutable
@Wrapped
abstract class _GroupId extends Wrapper<Integer> {
}